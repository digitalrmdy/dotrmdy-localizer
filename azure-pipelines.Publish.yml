# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines_Publish --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

variables:
  - group: MyGet-RMDY-Mobile-dotnet-localizer

trigger:
  batch: true
  tags:
    include:
      - '*.*.*'

stages:
  - stage: ubuntu_latest
    displayName: 'ubuntu-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: Publish
        displayName: 'Publish'
        steps:
          - checkout: self
            fetchDepth: 0
          - task: CmdLine@2
            inputs:
              script: './build.cmd Publish'
            env:
              MyGetFeedUrl: $(MyGetFeedUrl)
              MyGetApiKey: $(MyGetApiKey)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: artifacts
              pathtoPublish: 'artifacts'